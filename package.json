{
  "name": "lawchers-skills",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "packageManager": "npm@11.8.0",
  "engines": {
    "node": ">=20"
  },
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "npm run build --workspace @lawchers/shared-core && npm run build --workspace @lawchers/local-store && npm run build --workspace @lawchers/embedding-provider",
    "lint": "npm run typecheck",
    "test": "vitest run",
    "typecheck": "npm run build --workspace @lawchers/shared-core && npm run typecheck --workspace @lawchers/local-store && npm run typecheck --workspace @lawchers/embedding-provider"
  },
  "devDependencies": {
    "@types/node": "20.12.12",
    "@types/proper-lockfile": "4.1.4",
    "typescript": "5.4.5",
    "vitest": "1.6.0"
  }
}
