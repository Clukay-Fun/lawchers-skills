{
  "name": "lawchers-skills",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "packageManager": "npm@11.8.0",
  "engines": {
    "node": ">=20"
  },
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "npm run build --workspace @lawchers/cli",
    "lint": "npm run typecheck",
    "test": "vitest run",
    "typecheck": "npm run typecheck --workspace @lawchers/cli"
  },
  "devDependencies": {
    "@types/node": "20.12.12",
    "typescript": "5.4.5",
    "vitest": "1.6.0"
  }
}
