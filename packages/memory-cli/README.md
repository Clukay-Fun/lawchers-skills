# memory-cli

Planning placeholder for the future `memory` CLI.

Future commands:

```bash
memory learn --user <id> --user-message <text> --assistant-message <text>
memory recall --user <id> --query <text>
memory list --user <id>
memory clear --user <id>
memory sync-obsidian
memory doctor
```

See [Project 2](../../docs/projects/02-memory-cli.md).
